---
import { getUserAuth } from '@/utils/get-user-auth'
import TopBarHeader from '@/components/react/top-bar-header'
const user = await getUserAuth()

const isAdmin = user?.email === import.meta.env.EMAIL_ADMIN
---

<TopBarHeader client:visible user={user} isAdmin={isAdmin} />
